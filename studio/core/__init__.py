"""Core logic for the AI4Science Studio web GUI.

These modules only *read* repo metadata (models.yaml, model.yaml, sbatch headers)
and *execute* the tracked sbatch scripts unmodified. No ML dependencies.
"""
